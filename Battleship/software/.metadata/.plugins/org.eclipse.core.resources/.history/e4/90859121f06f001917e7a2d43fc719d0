#include "../gameVisualizer_bsp/system.h"

void main()
{
	int * start = GAMEMEMORY_AVALON_INTERFACE_0_BASE;
	for(int i = 0; i < GAMEMEMORY_AVALON_INTERFACE_0_SPAN / 16; i++)
	{
		*(start + i) = i;
	}
}
